Steps to follow to install the drivers of MC USB-1608G

1) sudo apt-get install build-essential

2) sudo apt-get install libudev-dev libusb-1.0-0-dev libfox-1.6-dev

3) git clone git://github.com/signal11/hidapi.git

 sudo apt-get install autotools-dev autoconf automake libtool
 cd hidapi
 sudo ./bootstrap
 sudo ./configure
 sudo make
 sudomake install


3) sudo cp 61-mcc.rules /etc/udev/rules.d
   sudo /sbin/udevadm control --reload-rules

4) Download MCCLIBUSB from ftp://lx10.tx.ncsu.edu/pub/Linux/drivers/USB/

 tar zxvf  MCCLIBUSB.1.2.tgz
 cd mcc-libusb
 sudo make
 sudo make install
 sudo ldconfig



